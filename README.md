 RequestForm.tsx
- Setup useReducer for form state management
- Integrated RTK Query mutations for request submission

key in maps
- fixed issues with react recieving key as component prop, when using it in map
- fixed route of request/id that's supposed to store requests of employee with id,
request id is generated by backend

TODO:
- Add approval logic and duplicate request validation
- request status update (pending, rejected, approved)
- vacation date validation (active, passed, upcoming)
