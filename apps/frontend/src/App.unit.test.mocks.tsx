import React from 'react';

export const signInPageModule = {
  default: () => <div data-testid="signin-page">SignIn</div>,
};

export const signUpPageModule = {
  default: () => <div data-testid="signup-page">SignUp</div>,
};

export const newPasswordPageModule = {
  default: () => <div data-testid="new-password-page">NewPassword</div>,
};

export const mainPageModule = {
  default: () => <div data-testid="main-page">Main</div>,
};

export const detailsPageModule = {
  default: () => <div data-testid="details-page">Details</div>,
};

export const rolesPageModule = {
  default: () => <div data-testid="roles-page">Roles</div>,
};

export const requestsPageModule = {
  default: () => <div data-testid="requests-page">Requests</div>,
};

export const createPageModule = {
  default: () => <div data-testid="create-page">Create</div>,
};
